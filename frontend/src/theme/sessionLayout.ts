/** MainView session card content column width — join/create buttons match this width */
export const SESSION_CARD_INNER_WIDTH_PX = 320;

/** Shared min-height for MainView + Create Session primary action buttons */
export const SESSION_BUTTON_MIN_HEIGHT_PX = 60;

/** Form field labels (e.g. Session Name, Participants Age) on Create Session */
export const SESSION_FORM_LABEL_FONT_SIZE_PX = 18;

/** Text inside the session name input and age dropdown */
export const SESSION_FORM_FIELD_FONT_SIZE_PX = 18;
